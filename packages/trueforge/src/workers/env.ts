/**
 * Worker bindings as generated from wrangler.jsonc by `pnpm workers:types`. Secrets such as
 * OIDC_CLIENT_SECRET are not bindings here; `config.ts` reads them from `process.env`.
 */
export type Env = Cloudflare.Env;
