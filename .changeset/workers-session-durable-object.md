---
'@truefoundry/trueforge': patch
---

Route turn start, streaming, subscribe, and cancel through a `TurnExecutor` port and add the Cloudflare Workers entry with a per-session Durable Object; no HTTP behavior change on Node.
